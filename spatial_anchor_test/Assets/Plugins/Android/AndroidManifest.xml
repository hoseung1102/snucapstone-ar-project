<?xml version="1.0" encoding="utf-8"?>
<!-- Unity 2022.3 LTS + ARDK 1.1.2 manifest
     - Launcher = com.rayneo.openxradapter.UnityOpenXrActivity (skiing game 패턴)
     - UnityOpenXrActivity extends RayNeoUnityPlayerActivity extends OpenXRActivity
       → super.onCreate chain 으로 OpenXRActivity.Initialize() 호출
       → mClient.bindService(MonitorService) + LauncherIPCManager.initIPC()
       → native loader 의 EstablishServiceConnection 성공
       → xrGetInstanceProcAddr 정상 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.vr.headtracking"
                  android:required="false"
                  android:version="1" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application>
        <!-- v0.8.4 fix: Android 11+ vendor namespace .so 접근에 명시 필요.
             helloar reference APK 에서 추출 (12개). 없으면 libQnnHtpV73Stub.so 가
             libcdsprpc.so dlopen 실패 → QnnClipEngine init 실패 → CLIP NPU 불가 → 광고 spawn X. -->
        <uses-native-library android:name="libcdsprpc.so" android:required="false" />
        <uses-native-library android:name="libadsprpc.so" android:required="false" />
        <uses-native-library android:name="libhidlbase.so" android:required="false" />
        <uses-native-library android:name="libhardware.so" android:required="false" />
        <uses-native-library android:name="libutils.so" android:required="false" />
        <uses-native-library android:name="libcutils.so" android:required="false" />
        <uses-native-library android:name="libc++.so" android:required="false" />
        <uses-native-library android:name="libbase.so" android:required="false" />
        <uses-native-library android:name="libprocessgroup.so" android:required="false" />
        <uses-native-library android:name="libhwbinder.so" android:required="false" />
        <uses-native-library android:name="libbinder_ndk.so" android:required="false" />
        <uses-native-library android:name="libandroid_runtime_lazy.so" android:required="false" />

        <!-- ARDK 의 UnityOpenXrActivity (skiing 게임이 사용하는 그것) 를 main launcher 로 -->
        <activity android:name="com.rayneo.openxradapter.UnityOpenXrActivity"
                  android:theme="@style/UnityThemeSelector"
                  android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|fontScale|layoutDirection|density"
                  android:hardwareAccelerated="false"
                  android:launchMode="singleTask"
                  android:resizeableActivity="false"
                  android:screenOrientation="landscape"
                  android:exported="true"
                  tools:replace="android:exported">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
        </activity>

        <!-- Mercury launcher 가 인식하는 metadata (skiing 게임 패턴) -->
        <meta-data android:name="com.rayneo.mercury.app" android:value="true" />
        <meta-data android:name="com.rayneo.mercury.support_key" android:value="true" />

        <!-- Unity 가 자동 추가하는 default UnityPlayerActivity 는 제거 -->
        <activity android:name="com.unity3d.player.UnityPlayerActivity"
                  tools:node="remove" />
    </application>
</manifest>
